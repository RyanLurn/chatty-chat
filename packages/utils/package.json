{
  "name": "@chatty-chat/utils",
  "type": "module",
  "scripts": {
    "start": "bun run src/index.ts",
    "try": "bun run tests/try.ts",
    "check-types": "tsc --noEmit"
  },
  "imports": {
    "#*": "./src/*.ts"
  },
  "dependencies": {
    "zod": "catalog:"
  },
  "devDependencies": {
    "@chatty-chat/tsconfigs": "workspace:*",
    "@types/bun": "catalog:types",
    "typescript": "catalog:"
  }
}
